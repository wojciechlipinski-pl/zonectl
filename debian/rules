#!/usr/bin/make -f

export PYBUILD_NAME=zonectl
export PYBUILD_SYSTEM=pyproject
export PYBUILD_TEST_ARGS=-q

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_test:
	PYTHONPATH=src python3 -m pytest -q
